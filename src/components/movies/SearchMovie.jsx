import { searchMovieAPI } from "../../helpers/moviesAPI";

export const SearchMovie = () =>{
    const formHandler = (evt) => {
      console.log('formHandler');  
    }
    return <p>SearchMovie</p>
}